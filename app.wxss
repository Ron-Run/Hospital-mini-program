@import"./styles/iconfont.wxss";
page,
text,
view,
scroll-view,
swiper,
swiper-item,
image,
navigator,
audio,
video {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}

page {
    /* 定义字体颜色 */
    --themeColor: #f9544e;
    /* 定义字体大小 */
    font-size: 28rpx;
}